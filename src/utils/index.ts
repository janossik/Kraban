export enum collectionName {
  USERS = "users",
  PROJECTS = "projects",
  COLUMNS = "columns",
  TASKS = "tasks",
}

export enum methodName {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum statusCode {
  OK = 200,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
}
